# WaypointFlit CRM — הקמה עצמאית (שלב 1: התחברות + אישור מנהל)

זה השלב הראשון בפרויקט. בסופו יהיה לכם אתר אמיתי, בכתובת משלכם, עם
התחברות במייל וסיסמה, ומסך ניהול שבו אתם (המנהל) מאשרים כל עובדת חדשה
לפני שהיא רואה נתונים.

## שלב 1 — יצירת פרויקט ב-Firebase (חינמי)

1. גשו ל-https://console.firebase.google.com והתחברו עם חשבון גוגל.
2. **Add project** → תנו שם, למשל `waypointflit-crm` → אפשר לכבות
   Google Analytics (לא נחוץ) → **Create project**.

## שלב 2 — הפעלת התחברות (Authentication)

1. בתפריט הצד: **Build → Authentication** → **Get started**.
2. בלשונית **Sign-in method**, בחרו **Email/Password** → הפעילו (Enable) → **Save**.

## שלב 3 — יצירת מסד נתונים (Firestore)

1. בתפריט הצד: **Build → Firestore Database** → **Create database**.
2. בחרו מיקום קרוב (למשל `eur3 (europe-west)`) → מצב **Production** → **Enable**.
3. בלשונית **Rules**, מחקו את מה שיש שם, והדביקו את התוכן המלא של הקובץ
   `firestore.rules` מהתיקייה הזו → **Publish**.

## שלב 4 — חיבור האתר לפרויקט

1. במסך הראשי של הפרויקט (Project Overview), לחצו על סמל ה-**</>** (Web app).
2. תנו כינוי, למשל `crm` → **Register app**. **לא** צריך Firebase Hosting SDK בשלב הזה.
3. יופיע קטע קוד עם `firebaseConfig = {...}`. העתיקו את הערכים (apiKey,
   authDomain, projectId וכו') לתוך הקובץ `firebase-config.js` שבתיקייה הזו,
   במקום הכיתוב "מלאו כאן".
4. באותו קובץ, ודאו שברשימת `ADMIN_EMAILS` מופיע המייל שאיתו תירשמו
   ראשונים כמנהל. שיניתי אותו מראש למייל ששימש בחיבור ל-Make
   (sh0559420955@gmail.com) — עדכנו אם זה לא המייל הנכון.

## שלב 5 — הרצה מקומית לבדיקה (לא חובה, אבל מומלץ)

הדרך הכי פשוטה בלי להתקין כלום: ב-VS Code, התקינו את התוסף
**Live Server**, פתחו את התיקייה `waypointflit-crm`, לחצו ימני על
`login.html` ← **Open with Live Server**.

## שלב 6 — העלאה לאוויר (Firebase Hosting, חינמי)

1. התקינו את כלי הפיתוח של Firebase (פעם אחת): פתחו טרמינל והריצו
   `npm install -g firebase-tools`.
2. בתיקיית הפרויקט: `firebase login` (ייפתח דפדפן להתחברות).
3. `firebase init hosting` → בחרו את הפרויקט שיצרתם → תיקיית ה-public:
   השאירו נקודה `.` (התיקייה הנוכחית) → Single-page app: **No** →
   אל תדרסו קבצים קיימים אם נשאלתם.
4. `firebase deploy` → בסיום תקבלו כתובת כמו
   `https://waypointflit-crm.web.app` — זו הכתובת הסופית של האתר.

## שלב 7 — יצירת המנהל הראשון (אתם)

1. פתחו את כתובת האתר → **הרשמה ראשונה** → הזינו את המייל שברשימת
   `ADMIN_EMAILS` וסיסמה → תיכנסו מיד כמנהל.

## שלב 8 — הזמנת עובדת

1. תבקשו מהעובדת להיכנס לאתר וללחוץ **הרשמה ראשונה** עם המייל שלה.
2. היא תראה מסך "ממתין לאישור מנהל".
3. אתם נכנסים, פותחים **מסך ניהול משתמשים** (קישור למעלה במסך הבית),
   ובוחרים לה תפקיד: **צוות** (עובדת רגילה) או **צפייה** (רק לראות).

זהו — שלב 1 שלם. בשלב הבא נבנה את מסך הלידים המלא (כרטיסים, סטטוסים,
סינון, מעקב, אחראי/ת) בתוך `index.html`, ואת השאר בהמשך.

## בעיות נפוצות
- **"Missing or insufficient permissions"** — כנראה לא הדבקתם את
  `firestore.rules` בקונסולה, או ששכחתם ללחוץ Publish.
- **מסך לבן / שגיאה בקונסול הדפדפן (F12)** — לרוב `firebase-config.js`
  לא מולא נכון. בדקו שאין רווחים או מרכאות חסרות.
- **לא מצליחים להריץ פקודות npm/firebase** — צריך Node.js מותקן
  במחשב (https://nodejs.org, גרסת LTS). אחרי ההתקנה סגרו ופתחו טרמינל
  מחדש.
